# Monitors
monitor=eDP-1,preferred,auto,1
monitor=,preferred,auto,1,mirror,eDP-1 # mirror htmi

# Execute programs at launch
exec-once = ~/.config/hypr/xdg-portal-hyprland
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = swww init

# Email configs
$mail01 = https://mail.google.com/mail/u/0/#inbox
$mail02 = https://mail.google.com/mail/u/1/#inbox
$mail03 = https://webmail.dias.ie/?_task=mail&_mbox=INBOX

# Launch apps
# exec-once = hyprctl dispatch workspace 1; sleep 1; slack & disown; firefox -new-window -url $mail01 -new-tab -url $mail02 -new-tab -url $mail03 & disown; sleep 2; hyprctl dispatch workspace 10; sleek & disown; sleep 1; hyprctl dispatch workspace 1

exec-once = waybar -c ~/.config/waybar/config.jsonc
exec-once = mako
exec-once = blueman-applet
exec-once = nm-applet --indicator
exec-once = wl-paste --watch cliphist store
exec-once = python3 ~/Main/horizons-wallpaper/horizonsWallpaper.py; swww img ~/.config/HyprV/backgrounds/$VER'-background.jpg' # horizons wallpaper
#exec-once = ~/.config/HyprV/hyprv_util setbg
exec-once = swww img ~/.config/HyprV/backgrounds/$VER'-background.jpg'

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = gb

    follow_mouse = 1

    touchpad {
        natural_scroll = yes
        disable_while_typing = true
        scroll_factor = 1.0
    }

    sensitivity = 0.6 # -1.0 - 1.0, 0 means no modification.
    accel_profile = flat
}

general {
    gaps_in = 5
    gaps_out = 14
    border_size = 5
    col.active_border=rgb(8fd6c9)
    col.inactive_border = rgb(394260)

    layout = dwindle
}

misc {
    disable_hyprland_logo = true
}

decoration {
    rounding = 0

    blur {
      enabled = true
      size = 7
      passes = 4

      blurls = lockscreen
    }

    drop_shadow = no
    shadow_range = 20
    shadow_render_power = 3
    col.shadow = rgb(394260)
}

animations {
    enabled = true

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
    bezier = myBezier, 0.10, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier, slide
    animation = windowsOut, 1, 7, myBezier, slide
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    new_is_master = true
}

gestures {
    workspace_swipe = true
}

misc {
  mouse_move_enables_dpms = true
  key_press_enables_dpms = true
}

#windowrule = float, ^(kitty)$
windowrule = float,^(pavucontrol)$
windowrule = float,^(blueman-manager)$
windowrule = float,^(nm-connection-editor)$
# windowrule = float,^(thunar)$
windowrule = float, title:^(btop)$
# windowrule = float, title:^(update-sys)$
windowrule = float, ^(matplotlib)$
windowrulev2 = float, title:^(.*cbonsai.*)$

windowrulev2 = animation popin,class:^(kitty)$,title:^(update-sys)$
windowrulev2 = animation popin,class:^(thunar)$
windowrulev2 = opacity 0.9 0.8,class:^(thunar)$
windowrulev2 = opacity 0.8 0.8,class:^(VSCodium)$
windowrulev2 = animation popin,class:^(firefox)$
windowrulev2 = move cursor -3% -105%,class:^(wofi)$
windowrulev2 = noanim,class:^(wofi)$
windowrulev2 = opacity 0.8 0.6,class:^(wofi)$
windowrulev2 = opacity 0.8 0.8, class:^(kitty)$
windowrulev2 = opacity 1 1,title:^(.*nvim.*)$
windowrulev2 = opacity 1 1,title:^(ncspot)$

$mainMod = SUPER
$modShift = SUPER_SHIFT

bind = $mainMod, Q, exec, kitty  #open the terminal
bind = $mainMod, F4, killactive, # close the active window
bind = $mainMod, L, exec, swaylock -f -i ~/Photos/Wallpapers/background-dark.jpg; hyprctl dispatch dpms off
# Bonsai :)
bind = $mainMod, T, exec, kitty --start-as=fullscreen cbonsai -b 2 -L 50 --time 10 -m 'See you soon :)' --screensaver
bind = $mainMod, M, exec, wlogout --protocol layer-shell # show the logout window
bind = $mainMod SHIFT, M, exit, # Exit Hyprland all together no (force quit Hyprland)
bind = $mainMod, E, exec, thunar # Show the graphical file browser
bind = $mainMod, V, togglefloating, # Allow a window to float
bind = $mainMod, F, fullscreen
bind = $mainMod, SPACE, exec, wofi # Show the graphical app launcher
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, S, exec, grim -g "$(slurp)" - | swappy -f - # take a screenshot
#bind = ALT, V, exec, cliphist list | wofi -dmenu | cliphist decode | wl-copy # open clipboard manager
bind = $mainMod, tab, workspace, e+1
bind = $modShift, tab, workspace, e-1
# open/close dashboard
bind = $mainMod, return, exec, ~/.config/eww/dashboard/launch_dashboard

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Toggle Waybar
bind = $mainMod, W, exec, killall -SIGUSR1 waybar

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf
source = ~/.config/hypr/media-binds.conf
source = ~/.config/hypr/env_var.conf
source = ~/.config/hypr/env_var_nvidia.conf
